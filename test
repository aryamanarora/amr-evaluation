:opX
different types of quantities
counts of role types for each AMR
individual verbs

syntactic parser: Stanford NLP Universal Dependencies parser
where the word lives syntax wise, does it affect pr/rc/f1
reentrancies (consider different kinds, e.g. He wants to fly, pronouns, coordinations)
multiple parsers, how it gets better or worse

uniform meaning representation (UMR)